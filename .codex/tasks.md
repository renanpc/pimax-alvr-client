# Future Tasks

- Clean up the temporary Java/Binder `PvrServiceClient` controller probe and ring-buffer fallback once the native Pimax `pxrapi` path is verified stable. Keep it until `sxrControllerStartTracking`/`sxrControllerGetState` has confirmed reliable controller pose, button, trigger, grip, and stick mapping.
